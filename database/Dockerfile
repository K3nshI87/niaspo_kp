# Используем официальный образ PostgreSQL
FROM postgres:13

# Копируем ваш SQL-скрипт в директорию контейнера
COPY init.sql /docker-entrypoint-initdb.d/

# Эта папка используется PostgreSQL для автоматического запуска всех скриптов в ней
# Скрипты будут автоматически выполнены при первом запуске контейнера PostgreSQL

# Параметры подключения к базе данных можно указать через ENV переменные (добавим их в docker-compose.yml)
